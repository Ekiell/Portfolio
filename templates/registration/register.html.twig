{% extends 'base.html.twig' %}

{% block body %}
    <div class="w-full max-w-sm p-6 m-auto mx-auto bg-white rounded-lg shadow-md">
        <div class="flex justify-center mx-auto">
            <img class="w-auto h-7 sm:h-8" src="https://merakiui.com/images/logo.svg" alt="">
        </div>

        {{ form_start(registrationForm) }}

        <div>
            {{ form_row(registrationForm.email, {'attr': {'class': 'block w-full px-4 py-2 mt-2 text-gray-700 bg-white border rounded-lg focus:border-blue-400 focus:ring-blue-300 focus:outline-none focus:ring focus:ring-opacity-40'}}) }}
        </div>

        <div class="mt-4">
            <div class="flex items-center justify-between">
                {{ form_label(registrationForm.plainPassword, 'Mot de passe', {'label_attr': {'class': 'block text-sm text-gray-800'}}) }}
                <a href="#" class="text-xs text-gray-600 hover:underline">Mot de passe oublié ? </a>
            </div>
            {{ form_widget(registrationForm.plainPassword, {'attr': {'class': 'block w-full px-4 py-2 mt-2 text-gray-700 bg-white border rounded-lg focus:border-blue-400 focus:ring-blue-300 focus:outline-none focus:ring focus:ring-opacity-40'}}) }}
        </div>

        <div class="mt-6">
            <button type="submit" class="w-full px-6 py-2.5 text-sm font-medium tracking-wide text-white capitalize transition-colors duration-300 transform bg-gray-800 rounded-lg hover:bg-gray-700 focus:outline-none focus:ring focus:ring-gray-300 focus:ring-opacity-50">
                S'inscrire
            </button>
        </div>

        {{ form_end(registrationForm) }}
    </div>
{% endblock %}
